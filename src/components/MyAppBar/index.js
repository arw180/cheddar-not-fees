import React, { Component } from 'react';
import { Link } from 'react-router';
import AppBar from 'material-ui/lib/app-bar';

/* component styles */
import { styles } from './styles.scss';

const MyAppBar = () => (
  <AppBar title="Cheddar Not Fees">
  </AppBar>
);

export default MyAppBar;
